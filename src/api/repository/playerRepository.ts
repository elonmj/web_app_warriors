import { Player } from '@/lib/Player';

export const getAllPlayers = async (): Promise<Player[]> => {
  throw new Error('Not implemented');
};

export const updatePlayerCategory = async (playerId: string, newCategory: string): Promise<void> => {
  throw new Error('Not implemented');
};